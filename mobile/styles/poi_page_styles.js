import { StyleSheet } from "react-native";

export const poiInfo = StyleSheet.create({
    // base layout
    layout: {
        width: "80%",
    },
    // image base
    poiImage: {
        borderRadius: 10,
        width: "100%",
        height: 200,
    },
});
